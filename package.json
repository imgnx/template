{
  "name": "taku-audio-frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "BUILD_TARGET=modular webpack serve --mode development",
    "dev:bitcrusher": "BUILD_TARGET=bitcrusher webpack serve --mode development",
    "dev:tuner": "BUILD_TARGET=tuner webpack serve --mode development",
    "dev:modular": "BUILD_TARGET=modular webpack serve --mode development",
    "build": "BUILD_TARGET=modular webpack --mode production",
    "build:bitcrusher": "BUILD_TARGET=bitcrusher webpack --mode production",
    "build:tuner": "BUILD_TARGET=tuner webpack --mode production",
    "build:modular": "BUILD_TARGET=modular webpack --mode production"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "@babel/preset-env": "^7.24.0",
    "@babel/preset-react": "^7.24.0",
    "babel-loader": "^9.1.3",
    "html-webpack-plugin": "^5.6.0",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  }
}
